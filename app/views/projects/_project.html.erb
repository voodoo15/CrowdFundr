  <% @projects.each do |project| %>
    <!-- <table id="project-index-table">
      <tr>
        <td id="index-project-name-desc"><p><%= link_to project.title, project_path(project.id) %></p>
          <p><%= project.short_description %></p></td>
        <td id="project-img"><img src="<%= project.image %>"></td>
    </table> -->

<div class="row">
  <div class="col s4 push-s8">
    <img src="<%= project.image %>">
  </div>
  <div class="col s8 pull-s4">
    <%= link_to project.title, project_path(project.id), id: "browse-project-title" %>
  </div>
</div>

<% end %>
